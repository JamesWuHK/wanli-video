# 🚀 启动说明

## 一键启动命令

```bash
# 确保你在项目根目录
cd /Users/wujames/cursor_prj/demo-video-generator

# 运行启动脚本
./start.sh
```

## 访问地址

启动成功后，打开浏览器访问：

- **主界面（Web UI）**: http://localhost
- **API 文档**: http://localhost:8001/docs
- **健康检查**: http://localhost:8001/health

## 系统服务

本系统包含 3 个 Docker 容器：

1. **frontend** - Web UI 前端（Nginx + React）
   - 端口: 80
   - 容器名: demo-video-frontend

2. **backend** - API 后端（FastAPI）
   - 端口: 8001
   - 容器名: demo-video-backend

3. **api** - 原有 CLI API（可选）
   - 端口: 8000
   - 容器名: demo-video-generator-api-1

## 常用命令

```bash
# 查看服务状态
docker-compose ps

# 查看所有日志
docker-compose logs -f

# 只查看前端日志
docker-compose logs -f frontend

# 只查看后端日志
docker-compose logs -f backend

# 停止所有服务
docker-compose down

# 重启服务
docker-compose restart

# 重新构建并启动
docker-compose up -d --build
```

## 使用流程

### 第一次使用

1. **访问首页**: http://localhost
2. **创建会话**:
   - 点击导航栏 "会话管理"
   - 点击 "创建会话" 按钮
   - 输入网站 URL，如: `https://essay.wanli.ai/`
   - （可选）填入 token 和 cookies
   - 点击确定

3. **分析网站**:
   - 点击导航栏 "网站分析"
   - 选择刚创建的会话
   - 输入要分析的 URL
   - 点击 "开始分析"
   - 等待分析完成（有进度条显示）

4. **生成脚本**:
   - 点击导航栏 "脚本管理"
   - 点击 "创建脚本"
   - 选择 AI 模型（DeepSeek 或 Claude）
   - 点击生成

5. **生成视频**:
   - 点击导航栏 "视频管理"
   - 选择已验证的脚本
   - 点击 "生成视频"
   - 等待生成完成（有进度显示）
   - 下载视频和字幕

## 环境变量配置

如果需要修改 API Keys 或其他配置：

```bash
# 编辑环境变量文件
vim backend/.env

# 修改后重启后端服务
docker-compose restart backend
```

## 故障排查

### 问题1: 无法访问 http://localhost

**可能原因**: 端口 80 被占用

**解决方案**: 修改端口
```bash
# 编辑 docker-compose.yml
vim docker-compose.yml

# 找到 frontend 服务的 ports 部分，改为：
# ports:
#   - "8080:80"

# 重启服务
docker-compose up -d

# 然后访问 http://localhost:8080
```

### 问题2: API 请求失败

**解决方案**:
```bash
# 1. 检查后端是否正常运行
docker-compose ps

# 2. 查看后端日志
docker-compose logs backend

# 3. 检查后端健康状态
curl http://localhost:8001/health

# 4. 重启后端
docker-compose restart backend
```

### 问题3: WebSocket 连接失败

**解决方案**:
```bash
# 重启前端服务
docker-compose restart frontend

# 查看前端日志
docker-compose logs frontend
```

### 问题4: 构建失败

**解决方案**:
```bash
# 清理旧容器和镜像
docker-compose down
docker system prune -a

# 重新构建（不使用缓存）
docker-compose build --no-cache

# 启动
docker-compose up -d
```

## 数据存储位置

- 数据库文件: `backend/data/app.db`
- 生成的视频: `backend/data/videos/`
- CLI 输出: `output/`

这些目录会自动创建，数据会持久保存。

## 停止系统

```bash
# 停止所有服务（保留数据）
docker-compose down

# 停止并删除所有数据
docker-compose down -v
```

## 技术支持

如有其他问题，请查看：
- [完整文档](FULLSTACK_QUICKSTART.md)
- [前端文档](frontend/README.md)
- [API 文档](http://localhost:8001/docs)

---

**祝使用愉快！** 🎉
